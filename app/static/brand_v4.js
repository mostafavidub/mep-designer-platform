(()=>{const reduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const targets=[...document.querySelectorAll('.section,.blog-card,.article-wrap h2,.article-featured,.article-visual-grid figure')];targets.forEach((el,i)=>{el.classList.add('reveal-brand');if(reduced)el.classList.add('is-visible');else el.style.transitionDelay=`${Math.min(i%4,3)*45}ms`});if(!reduced&&'IntersectionObserver'in window){const io=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}})},{threshold:.08,rootMargin:'0px 0px -6% 0px'});targets.forEach(el=>io.observe(el))}else targets.forEach(el=>el.classList.add('is-visible'));const path=location.pathname;document.querySelectorAll('.top-nav a').forEach(a=>{const href=a.getAttribute('href');if(href==='/'?path==='/':path.startsWith(href)){a.setAttribute('aria-current','page')}});document.querySelectorAll('.service-cta,.trust-grid article,.deliverable-grid article,.limit-grid article').forEach((el,i)=>el.setAttribute('data-brand-code',`ET / ${String(i+1).padStart(2,'0')}`));})();
