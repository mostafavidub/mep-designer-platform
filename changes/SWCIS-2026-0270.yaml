{
  "change_id": "SWCIS-2026-0270",
  "summary": "Keep health, session and panel APIs responsive during CPU-heavy architecture questionnaire analysis",
  "requester": "project-owner",
  "reference_inventory_authorization": "2026-09-08: private reference projects are optional; automated regression remains required.",
  "owner_release_authorization": "2026-09-21: owner requested complete correction of the recurring Staging project-file error; Production remains untouched.",
  "change_types": ["ui_api", "test", "documentation", "governance", "deployment"],
  "changed_rule_ids": [],
  "impacted_modules": ["ui_api", "qa", "docs", "governance", "manifest", "migration", "versioning", "deployment"],
  "impact_notes": {
    "ui_api": "changed: CPU-heavy questionnaire reconstruction is isolated from the asynchronous web event loop without changing the endpoint contract",
    "qa": "changed: regression proves the blocking analysis path is offloaded and existing endpoint destructive checks remain active",
    "docs": "added: questionnaire runtime responsiveness incident, contract, verification and rollback evidence",
    "governance": "verified: no engineering rule, calculation, inference or release gate is weakened",
    "manifest": "verified: build identity and artifact manifests are unchanged",
    "migration": "verified: no stored data or API payload migration is required",
    "versioning": "verified: behavior-preserving runtime correction requires no schema or SWCIS version bump",
    "deployment": "changed: Staging web-service rollout with concurrent health/session verification; Production untouched"
  },
  "evidence": {
    "traceability": ["project upload -> questionnaire endpoint -> worker-thread DXF reconstruction -> unchanged inferred answers/questions -> panel"],
    "tests": ["tests/test_unified_questionnaire_v5.py", "complete repository regression suite"],
    "docs": ["docs/QUESTIONNAIRE_RUNTIME_RESPONSIVENESS.md"],
    "version_bump": ["not required: endpoint and engineering semantics are unchanged; scheduling defect only"],
    "migration": ["not required: no persisted contract changed"],
    "golden_regression": ["existing architecture, questionnaire, panel and mechanical regression suites remain mandatory"],
    "semantic_diff": ["event-loop-blocking reconstruction -> identical reconstruction in the bounded worker thread pool"],
    "artifact_diff": ["not affected: no drawing or downloadable artifact mutation"],
    "numeric_diff": ["not affected: analysis values and calculations are unchanged"],
    "contract_diff": ["response contract unchanged; runtime responsiveness contract added"],
    "deployment_plan": ["deploy approved commit to Staging web service; verify health and customer session remain responsive during real-file analysis"],
    "rollback_plan": ["redeploy the prior approved Staging Git commit"]
  },
  "risk": {"likelihood": 3, "severity": 4, "detectability": 2, "score": 24, "rationale": "Event-loop starvation blocks every customer flow but is directly detectable through concurrent health/session probes and worker logs."},
  "waiver": null
}
