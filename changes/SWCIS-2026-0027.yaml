{
  "change_id": "SWCIS-2026-0027",
  "summary": "Supervise and automatically restart the co-built CAD runtime so paid projects cannot lose the internal design service while the public web process remains healthy.",
  "requester": "mostafavidub",
  "change_types": ["test", "governance", "deployment"],
  "changed_rule_ids": [],
  "impacted_modules": ["deployment", "docs", "governance", "manifest", "migration", "qa", "ui_api", "versioning"],
  "impact_notes": {
    "manifest": "No drawing-manifest semantics change; approved manifests reach the same locked engine after runtime recovery.",
    "ui_api": "The existing public workflow and progress API retain their contracts while recovering availability of their internal CAD dependency.",
    "qa": "Startup contract test requires an explicit restart loop and preserves all downstream release gates.",
    "versioning": "Deployment identity remains Git/content-derived.",
    "migration": "No data or schema migration.",
    "docs": "This change request records the co-built runtime availability correction.",
    "governance": "Runtime recovery restores availability only and does not convert QA failures into successful releases.",
    "deployment": "Deploy the supervised two-process container, verify CAD reachability, and retry the preserved paid project in place."
  },
  "evidence": {
    "tests": ["tests/test_cad_service_supervision.py", "full suite required before merge"],
    "docs": ["changes/SWCIS-2026-0027.yaml"],
    "version_bump": ["Git/content-derived deployment identity"],
    "golden_regression": ["CAD subprocess exits while the public web process remains active"],
    "semantic_diff": ["Unexpected CAD child termination now triggers automatic restart"],
    "artifact_diff": ["not affected"],
    "contract_diff": ["No external API or artifact contract change"],
    "deployment_plan": ["deploy; verify /system_health reports CAD reachable; retry PRJ-7253354 without charging"],
    "migration": ["none"],
    "rollback_plan": ["revert the commit; paid project data and uploads remain preserved"]
  },
  "risk": {"likelihood": 1, "severity": 4, "detectability": 1, "score": 4, "rationale": "The loop supervises only the canonical local CAD child and container shutdown still terminates the process namespace."},
  "waiver": null
}
