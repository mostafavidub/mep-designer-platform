{
  "change_id": "SWCIS-2026-0028",
  "summary": "Run the canonical CAD route in the public process on memory-constrained Railway deployments, preventing duplicate Python runtimes from being killed during paid project generation.",
  "requester": "mostafavidub",
  "change_types": ["ui_api", "drawing_output", "test", "governance", "deployment"],
  "changed_rule_ids": [],
  "impacted_modules": ["cad_designer", "deployment", "docs", "governance", "manifest", "migration", "qa", "ui_api", "versioning"],
  "impact_notes": {
    "cad_designer": "The canonical DesignRequest and design route execute in-process only when the production flag is enabled.",
    "ui_api": "The public workflow receives the same response and failure structure without a localhost network hop.",
    "manifest": "The same design route enforces the approved manifest and all existing count gates.",
    "qa": "HTTPException failures are preserved as non-success responses; locked QA is not weakened.",
    "versioning": "The canonical entrypoint is imported before dispatch and retains Git/content-derived identity.",
    "migration": "No data or schema migration.",
    "docs": "This change request records the Railway memory topology correction.",
    "governance": "Only process topology changes; design authority and release criteria remain identical.",
    "deployment": "Enable in-process CAD in the production image, verify integrated health, retry the preserved project, and require downloadable output."
  },
  "evidence": {
    "tests": ["tests/test_cad_runtime_recovery.py", "tests/test_cad_service_supervision.py", "full suite required before merge"],
    "docs": ["changes/SWCIS-2026-0028.yaml"],
    "version_bump": ["Git/content-derived deployment identity"],
    "golden_regression": ["30 MB paid architecture upload in a memory-constrained Railway container"],
    "semantic_diff": ["Canonical /design implementation is invoked directly instead of through a duplicate localhost Python process"],
    "artifact_diff": ["Expected DXF and QA contract unchanged"],
    "contract_diff": ["Public API unchanged; internal HTTP exception semantics retained"],
    "deployment_plan": ["deploy; verify integrated health; retry PRJ-7253354 without charging; validate stored artifact and download"],
    "migration": ["none"],
    "rollback_plan": ["unset COBUILT_CAD_IN_PROCESS and revert this commit; stored project inputs remain intact"]
  },
  "risk": {"likelihood": 2, "severity": 4, "detectability": 1, "score": 8, "rationale": "Execution topology changes, but it uses the same typed request, canonical function, response schema, and locked QA path."},
  "waiver": null
}
