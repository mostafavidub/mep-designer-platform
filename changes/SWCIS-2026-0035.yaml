{
  "change_id": "SWCIS-2026-0035",
  "summary": "Keep the generated runtime rulebook on the panel volume so startup cannot mutate a packaged release-identity input shared with the dedicated CAD service.",
  "requester": "mostafavidub",
  "change_types": ["release_identity", "test", "governance", "deployment"],
  "changed_rule_ids": [],
  "impacted_modules": ["cad_designer", "deployment", "docs", "governance", "manifest", "migration", "qa", "ui_api", "versioning"],
  "impact_notes": {
    "cad_designer": "Panel and CAD now hash the same immutable packaged rulebook bytes.",
    "ui_api": "No public API or UI behavior changes.",
    "manifest": "Runtime identity no longer changes when the panel generates its working rulebook.",
    "qa": "The strict runtime contract gate remains enabled and compares stable inputs.",
    "versioning": "The content-derived rulebook hash remains equal across services built from one commit.",
    "migration": "The panel creates or refreshes its working rulebook under the existing /data volume.",
    "docs": "This change request records the release-identity isolation.",
    "governance": "Packaged governance evidence stays immutable after image creation.",
    "deployment": "Deploy both services from the same commit and retry the preserved paid project."
  },
  "evidence": {
    "tests": ["tests/test_dedicated_cad_deployment.py", "full suite required before merge"],
    "docs": ["changes/SWCIS-2026-0035.yaml"],
    "version_bump": ["Automatic identity changes with Git commit"],
    "golden_regression": ["Panel startup cannot overwrite data/rulebook inside the packaged image"],
    "semantic_diff": ["Working rulebook path moves from /app/data to the persistent /data volume"],
    "artifact_diff": ["Output contract unchanged"],
    "contract_diff": ["Cross-service rulebook_hash stays equal for the same source"],
    "deployment_plan": ["deploy both services from this commit; verify health; retry PRJ-7253354"],
    "migration": ["none; working rulebook is regenerated on startup"],
    "rollback_plan": ["revert commit and co-locate services; stored projects remain intact"]
  },
  "risk": {"likelihood": 1, "severity": 4, "detectability": 1, "score": 4, "rationale": "Changes only the generated working-copy location and is guarded by an explicit immutability test."},
  "waiver": null
}
