{
  "change_id": "SWCIS-2026-0036",
  "summary": "Remove service-local Railway deployment IDs from the shared build identity so independently deployed panel and CAD containers remain contract-compatible.",
  "requester": "mostafavidub",
  "change_types": ["release_identity", "test", "governance", "deployment"],
  "changed_rule_ids": [],
  "impacted_modules": ["cad_designer", "deployment", "docs", "governance", "manifest", "migration", "qa", "ui_api", "versioning"],
  "impact_notes": {
    "cad_designer": "Cross-service identity uses source identity rather than a service-specific deployment identifier.",
    "ui_api": "No public API or UI behavior changes.",
    "manifest": "Fallback build timestamp is deterministic across services.",
    "qa": "The strict runtime contract gate remains enabled and now rejects only meaningful release differences.",
    "versioning": "Unknown-commit fallback is stable across independently deployed services.",
    "migration": "No stored-data migration.",
    "docs": "This change request records deterministic cross-service identity.",
    "governance": "No required identity field is removed; only the invalid service-local fallback source is excluded.",
    "deployment": "Deploy both services from the same commit and retry the preserved paid project."
  },
  "evidence": {
    "tests": ["tests/test_single_living_system.py", "full suite required before merge"],
    "docs": ["changes/SWCIS-2026-0036.yaml"],
    "version_bump": ["Automatic identity changes with Git commit"],
    "golden_regression": ["Distinct Railway deployment IDs produce identical fallback build timestamps"],
    "semantic_diff": ["RAILWAY_DEPLOYMENT_ID no longer participates in shared release identity"],
    "artifact_diff": ["Artifact metadata retains the complete build identity schema"],
    "contract_diff": ["Cross-service build_identity can match when commit environment metadata is unavailable"],
    "deployment_plan": ["deploy both services from this commit; verify health; retry PRJ-7253354"],
    "migration": ["none"],
    "rollback_plan": ["revert commit and co-locate services; stored projects remain intact"]
  },
  "risk": {"likelihood": 1, "severity": 4, "detectability": 1, "score": 4, "rationale": "Keeps all governed hashes and revisions while replacing only a non-portable fallback value, with regression coverage."},
  "waiver": null
}
