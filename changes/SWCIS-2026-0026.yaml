{
  "change_id": "SWCIS-2026-0026",
  "summary": "Treat an empty Railway DATABASE_URL as unset and retain the persistent SQLite volume default so production can start after deployment.",
  "requester": "mostafavidub",
  "change_types": ["ui_api", "test", "governance", "deployment"],
  "changed_rule_ids": [],
  "impacted_modules": ["deployment", "docs", "governance", "manifest", "migration", "qa", "ui_api", "versioning"],
  "impact_notes": {
    "ui_api": "Application startup now resolves an empty DATABASE_URL to the existing DATA_DIR SQLite database instead of passing an invalid URL to SQLAlchemy.",
    "manifest": "No drawing-manifest behavior changes; the application can reach the existing manifest workflow after startup.",
    "qa": "A subprocess regression test reproduces the exact empty environment variable used by Railway.",
    "versioning": "Deployment identity remains Git/content-derived.",
    "migration": "No schema or data migration; the existing /data/mep.db location remains unchanged.",
    "docs": "This change request records the production environment fallback.",
    "governance": "The startup correction does not bypass any CAD or release QA gate.",
    "deployment": "Deploy only after tests and SWCIS pass, verify health, then retry the paid project in place."
  },
  "evidence": {
    "tests": ["tests/test_empty_database_url_fallback.py", "full suite required before merge"],
    "docs": ["changes/SWCIS-2026-0026.yaml"],
    "version_bump": ["Git/content-derived deployment identity"],
    "golden_regression": ["Railway environment with DATABASE_URL set to an empty string"],
    "semantic_diff": ["Empty and absent DATABASE_URL now have the same safe persistent-SQLite meaning"],
    "artifact_diff": ["not affected"],
    "contract_diff": ["No external contract change"],
    "deployment_plan": ["deploy; verify /health; retry PRJ-7253354 without charging"],
    "migration": ["no migration; reuse /data/mep.db"],
    "rollback_plan": ["revert the commit; existing database remains untouched"]
  },
  "risk": {"likelihood": 1, "severity": 3, "detectability": 1, "score": 3, "rationale": "The fallback is limited to an unusable empty value and preserves the established persistent database path."},
  "waiver": null
}
