{
  "change_id": "SWCIS-2026-0033",
  "summary": "Derive cross-service build identity from the shared immutable Git commit instead of service-local Railway deployment IDs.",
  "requester": "mostafavidub",
  "change_types": ["release_identity", "test", "governance", "deployment"],
  "changed_rule_ids": [],
  "impacted_modules": ["cad_designer", "deployment", "docs", "governance", "manifest", "migration", "qa", "ui_api", "versioning"],
  "impact_notes": {
    "cad_designer": "Panel and CAD services built from the same commit now produce the same build identity.",
    "ui_api": "No public contract change.",
    "manifest": "Runtime gate still compares complete release identity and content hashes.",
    "qa": "Actual commit, schema, rulebook, manufacturer, compliance, and dependency drift still fail closed.",
    "versioning": "Build timestamp fallback becomes commit-stable across services; explicit BUILD_TIMESTAMP and Git commit time retain precedence.",
    "migration": "No stored-data migration.",
    "docs": "This change request records multi-service identity semantics.",
    "governance": "The runtime gate is corrected for one logical release deployed across multiple services, not weakened.",
    "deployment": "Deploy both services from the same commit before retrying the project."
  },
  "evidence": {
    "tests": ["tests/test_single_living_system.py", "full suite required before merge"],
    "docs": ["changes/SWCIS-2026-0033.yaml"],
    "version_bump": ["Automatic identity changes with the Git commit"],
    "golden_regression": ["Same commit with different Railway deployment IDs yields identical identity"],
    "semantic_diff": ["Service-local deployment ID no longer creates false cross-service drift"],
    "artifact_diff": ["Artifact build stamp remains complete and commit-addressed"],
    "contract_diff": ["build_timestamp fallback is now commit:<sha> when deployment images omit .git"],
    "deployment_plan": ["deploy web-app and dedicated CAD from the same commit; verify runtime gate; retry PRJ-7253354"],
    "migration": ["none"],
    "rollback_plan": ["revert commit and co-locate services; stored project and artifacts remain intact"]
  },
  "risk": {"likelihood": 1, "severity": 5, "detectability": 1, "score": 5, "rationale": "Identity remains fail-closed on immutable commit and all content/schema hashes, with focused regression coverage."},
  "waiver": null
}
